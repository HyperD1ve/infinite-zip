services:
  - type: web
    name: infinite-zip
    runtime: node
    plan: free
    buildCommand: npm install && python3 -m pip install --user -r requirements-ml.txt
    startCommand: npm start
    autoDeploy: false
    envVars:
      - key: NODE_ENV
        value: production
