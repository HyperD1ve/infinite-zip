# Models

Generated quality-model artifacts live here.

Default files produced by `npm run train:model`:

```text
puzzle_quality.ubj
puzzle_quality-metadata.json
puzzle_quality-feature-importance.csv
```

These artifacts are ignored by git because they can be regenerated from the training CSV. Keep this README tracked so the directory has a documented purpose.
